<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.mgusev.eldritchhorror">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ui.activity.main.MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.pager.PagerActivity">
        </activity>

        <activity android:name=".ui.activity.pager.InvestigatorActivity"
            android:theme="@style/DialogTheme">
        </activity>
        <activity android:name=".ui.activity.pager.ExpansionChoiceActivity" android:theme="@style/DialogTheme"/>
        <activity android:name=".ui.activity.details.DetailsActivity" />
        <activity android:name=".ui.activity.about.AboutActivity" />
    </application>

</manifest>